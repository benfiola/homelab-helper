apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: {{ include "gateway-controller.name" . }}
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: {{ include "gateway-controller.name" . }}
subjects:
  - kind: ServiceAccount
    name: {{ include "gateway-controller.name" . }}
    namespace: {{ .Release.Namespace }}
